# Example only. Prefer the README's --from-file command so secrets do not enter
# shell history. Never commit a populated copy of this file.
apiVersion: v1
kind: Secret
metadata:
  name: claw-redteam-secrets
  namespace: redteam
type: Opaque
stringData:
  OPENCLAW_GATEWAY_TOKEN: REPLACE_ME
  ATTACKER_API_KEY: REPLACE_ME
